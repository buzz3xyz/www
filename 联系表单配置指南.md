# 联系我们表单 — 配置指南

> 站点是 GitHub Pages 上的纯静态页面，没有自己的后端。所以表单需要一个**第三方的接收端点**来把提交转成邮件或消息。

---

## 一、先说结论

**前端已经全部做完了，只差一个「接收地址」。**

打开 `buzz3.js`，第 11 行：

```js
formEndpoint: '',
```

把它填成下面任意一个方案的地址，联系表单立刻可用。**除此之外不需要改任何代码。**

改之前表单会显示一句诚实的提示（「尚未配置」），而不是假装发送成功——这是故意的，免得你以为能用其实收不到。

---

## 二、已经做好的部分（不用你操心）

| 能力 | 说明 |
|---|---|
| 字段与校验 | 姓名、邮箱、公司/项目、主题、留言；必填与长度都在前端校验，逐字段提示 |
| 蜜罐 | `#cfWebsite` 对真人隐藏，机器人会填。填了就假装成功，让爬虫继续往空处发 |
| 填表耗时 | 少于 3.5 秒提交判为机器人 |
| 链接数 | 留言里超过 5 个链接判为垃圾 |
| 黑名单 | `seo services`、`guest post`、`casino` 等常见垃圾词组 |
| 频率限制 | 同一浏览器 1 分钟 1 次、1 小时 5 次 |
| 成功态 | 提交成功后表单收起，显示「已发送」面板 |
| 多语言 | 中 / 英 / 日 三语，错误提示都跟着语言走 |

---

## 三、表单实际发出的数据

提交时 POST 的 JSON body 长这样：

```json
{
  "name": "张三",
  "email": "zhangsan@example.com",
  "organization": "Acme",
  "topic": "partnership",
  "message": "留言正文……",
  "lang": "zh",
  "page": "https://www.buzz3.xyz/#contact",
  "subject": "……",
  "turnstileToken": "……"
}
```

- `topic` 取值：`general` / `partnership` / `consulting` / `media` / `other`
- `subject` 只在填了 `formSubject` 时才带
- `turnstileToken` 只在配了 Turnstile 时才带

**两个关键字段，两家服务都认：**

- `email` → 通知邮件的 **Reply-To**（你直接点「回复」就能回到访客）
- `subject` → 通知邮件的 **主题**

---

## 四、方案 A：Formspree

适合不想自己运维的人。免费额度每月 50 条，超出需付费。

**步骤：**

1. 打开 <https://formspree.io> 注册（可用 GitHub 登录）。
2. 控制台里 **New Form**，名字随便填，比如 `buzz3 contact`。
3. 建好后页面会给你一个形如 `https://formspree.io/f/xxxxxxxx` 的地址，复制它。
4. 在 Formspree 里进入 **Settings → Allowed Domains**，填上：
   ```
   buzz3.xyz
   www.buzz3.xyz
   ```
   > 这一步**建议做**。因为端点地址写在公开的 JS 里，任何人都能看到；加了域名白名单，别人就算抄走地址也没法从自己的站点滥用你的额度。
5. 回到 `buzz3.js` 填上：
   ```js
   formEndpoint: 'https://formspree.io/f/xxxxxxxx',
   formSubject: 'Buzz3 网站联系表单',
   ```
6. 部署，然后按第八节验证。

**注意：** 第一次提交后 Formspree 会给你发一封确认邮件，**必须点一下确认**，否则后续提交不会转发给你。这是最常被漏掉的一步。

---

## 五、方案 B：Web3Forms

免费额度更宽松（每月 250 条），不需要注册账号，只要一个 Access Key。

**步骤：**

1. 打开 <https://web3forms.com>，在首页输入你要接收邮件的邮箱，点 **Create Access Key**。
2. 邮箱里会收到一个 UUID 形式的 Access Key，形如 `a1b2c3d4-1234-5678-9abc-def012345678`。
3. 在 `buzz3.js` 里填上：

   ```js
   formEndpoint: 'https://api.web3forms.com/submit/a1b2c3d4-1234-5678-9abc-def012345678',
   formSubject: 'Buzz3 网站联系表单',
   ```

   > ⚠️ **Access Key 必须写在 URL 路径里**，不能只写 `https://api.web3forms.com/submit`。
   >
   > 官方文档给了两种传法：一是把 key 放在请求 body 的 `access_key` 字段里，二是直接拼在 URL 上。我们的代码发的是自定义字段（`name` / `email` / `message` …），**不会**带 `access_key` 字段，所以只能用第二种——拼在 URL 上。写成第一种的地址会直接报 400。

4. Web3Forms 也有域名限制设置，建议在它的控制台里限制来源域名，理由同方案 A。
5. 部署，然后按第八节验证。

---

## 六、方案 C：自建端点

任何「收 POST、返回 2xx」的地址都可以。适合你已经有 Cloudflare Worker / Vercel / 自建服务器的情况。

**接口契约：**

| 项目 | 要求 |
|---|---|
| 方法 | `POST` |
| 请求头 | `Content-Type: application/json` |
| 请求体 | 第三节的 JSON |
| 成功 | 返回 **2xx** 状态码（body 内容不限，前端只看状态码） |
| 失败 | 返回非 2xx，前端会提示「发送失败」 |
| **CORS** | **必须返回 `Access-Control-Allow-Origin`**，允许 `https://www.buzz3.xyz`；且必须正确处理 `OPTIONS` 预检请求 |

> **CORS 是最容易踩的坑。** 页面在 `www.buzz3.xyz`，请求打到你的域名，浏览器会先发 `OPTIONS` 预检。如果你的端点没返回正确的 `Access-Control-Allow-Origin`，请求会在浏览器里被拦掉，前端只会看到「发送失败」，服务器日志里**什么都看不到**——因为请求根本没发出去。

一个最小可用的 Cloudflare Worker 示例（把收到的内容转发到你邮箱，用 Resend / MailChannels 之类）：

```js
export default {
  async fetch(request, env) {
    const cors = {
      'Access-Control-Allow-Origin': 'https://www.buzz3.xyz',
      'Access-Control-Allow-Methods': 'POST, OPTIONS',
      'Access-Control-Allow-Headers': 'Content-Type, Accept',
    };
    if (request.method === 'OPTIONS') return new Response(null, { headers: cors });
    if (request.method !== 'POST') return new Response('Method Not Allowed', { status: 405, headers: cors });

    const data = await request.json();

    // 服务端也要自己验一遍，不要只信前端
    if (!data.name || !data.email || !data.message) {
      return new Response('Bad Request', { status: 400, headers: cors });
    }

    await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${env.RESEND_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        from: 'noreply@buzz3.xyz',
        to: 'hello@buzz3.xyz',
        reply_to: data.email,
        subject: data.subject || `网站联系表单：${data.topic}`,
        text: `姓名：${data.name}\n邮箱：${data.email}\n公司：${data.organization}\n主题：${data.topic}\n\n${data.message}`,
      }),
    });

    return new Response('OK', { status: 200, headers: cors });
  },
};
```

---

## 七、可选：再补两项

### 7.1 `formSubject` — 通知邮件的主题

```js
formSubject: 'Buzz3 网站联系表单',
```

不填的话，邮件主题由服务商自己决定（Formspree 会拿第一个字段凑一个）。填上更好认。

### 7.2 `turnstileSiteKey` — 加一道人机验证

上面的蜜罐、耗时、频率限制都在**浏览器里**跑，脚本可以绕过。Turnstile 是唯一真正能证明「对面是人」的一层，由 Cloudflare 在服务端校验。

```js
turnstileSiteKey: '0x4AAAAAAA...',
```

- 在 <https://dash.cloudflare.com> → **Turnstile** → Add Site 免费拿 Site Key
- 填上之后，弹窗里会自动出现验证控件，token 随 `turnstileToken` 字段一起发出去
- **端点那一侧还需要拿 secret key 去校验**，这步在你的服务端做（Formspree / Web3Forms 目前都不代验）
- 不填的话，页面**一个第三方请求都不会发**——没配就不加载任何外部脚本

> 建议：先用方案 A 或 B 把功能跑起来，等真的开始收到垃圾邮件了，再上 Turnstile。它是「可选加固」，不是「上线必需」。

---

## 八、改完怎么验证

1. 本地起服务，打开页面，滚动到 CTA 区，点 **Let's Talk**。
2. 正常填一遍，提交。应该看到表单收起、出现「已发送」。
3. **去接收邮箱确认收到**——并且直接点「回复」，看收件人是不是访客填的邮箱（这验证 `email` → Reply-To 生效了）。
4. 故意少填一个必填项，应该看到逐字段的红字提示，而不是笼统的失败。
5. 打开浏览器开发者工具 → Network，看那条 `POST` 请求：
   - 状态码应该是 **2xx**
   - 如果状态是 `(failed)` / `CORS error`，就是第六节说的跨域问题
   - 如果是 **404**，地址写错了
   - 如果是 **400**，多半是 Web3Forms 的 key 没拼进 URL
   - 如果是 **429**，是触发了频率限制，等一分钟再试
6. 确认没问题后，把 `formEndpoint` 提交推送。

---

## 九、安全与隐私

- **端点地址是公开的。** 它写在 `buzz3.js` 里，任何人打开源码都能看到。这不是漏洞，但意味着**必须配域名白名单**（方案 A / B 都支持），否则别人可以拿你的额度发自己的垃圾邮件。
- **不要在前端放任何密钥。** `turnstileSiteKey` 是设计上公开的（Site Key 本来就要给浏览器），但 Turnstile 的 **Secret Key 绝不能写进 `buzz3.js`**，它只能待在你的服务端。
- **服务端要自己再验一遍。** 前端校验只防手滑，不防恶意。长度、必填、格式都要在接收端重做。
- **隐私文案已就位。** 弹窗底部写着「我们只用你的信息回复，不订阅、不外传」。如果以后接了第三方 CRM 或邮件列表，记得同步改这句——不然就是文案说谎了。
- **垃圾邮件预算。** 现有 5 层客户端防护挡得住绝大多数自动化提交。如果哪天还是被刷，再上 Turnstile。

---

## 十、回滚

把 `formEndpoint` 改回 `''` 即可。表单会退回「尚未配置」的提示状态，不会再往任何地方发请求，也不会报错。
